export type IinitialState = {
  questions: [];
  status: string;
  index: number;
  points: number;
};

export interface DataType {
  numQuestions: number;
  category: string;
  difficulity: string;
}
